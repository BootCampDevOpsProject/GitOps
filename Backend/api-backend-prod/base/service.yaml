apiVersion: v1
kind: Service
metadata:
  name: api-backend-production-svc
  labels:
    app: api-backend-production
spec:
  ports:
    - name: http
      port: 80
      targetPort: 5000
  selector:
    app: api-backend-production
  type: ClusterIP
